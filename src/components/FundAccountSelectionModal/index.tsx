import ModalShell from "@/components/ModalShell/ModalShell";

const FundAccountSelectionModal = ({ open, width, onClose }) => {
  return (
    <ModalShell open={} title="Fund Account">
      <></>
    </ModalShell>
  );
};

export default FundAccountSelectionModal;
