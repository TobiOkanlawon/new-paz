info:
  name: Customer Loans
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/v1/user/loan/pending"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
