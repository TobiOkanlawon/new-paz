info:
  name: Loans Attached To Wallet
  type: http
  seq: 8

http:
  method: GET
  url: "{{base_url}}/v1/loans/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
