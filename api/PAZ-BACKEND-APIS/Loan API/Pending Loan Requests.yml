info:
  name: Pending Loan Requests
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/v1/user/loan/request/pending"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
